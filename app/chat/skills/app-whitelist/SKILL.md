---
name: app-whitelist
description: 给 APPID 加白，开通 KOL/展厅测试员权限。用户要求 APPID 加白或开通展厅测试权限时使用；执行前须用户确认。
tools:
  - add_users_to_app
react_hint: APPID 加白：add_users_to_app（需用户前端确认；每行一个 APPID）
casual_capability: add_users_to_app：APPID 加白（KOL/展厅测试员）
keywords:
  - appid加白
  - app加白
  - 加白
  - 展厅测试
  - kol
---
# APPID 加白

用户给 APPID 加白（KOL/展厅测试员权限）→ `add_users_to_app`。

每行一个 APPID，如 `1234567890` 或 `1234567890 张三`
